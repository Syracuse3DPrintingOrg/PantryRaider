name = "foodassistant-dashboard"
main = "worker.js"
compatibility_date = "2024-01-01"

[vars]
GITHUB_OWNER = "Syracuse3DPrintingOrg"
GITHUB_REPO  = "PantryRaider"

# Add GITHUB_TOKEN as an encrypted secret in Cloudflare dashboard:
#   Worker Settings → Variables and Secrets → Add → GITHUB_TOKEN
